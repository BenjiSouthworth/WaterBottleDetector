Dubhacks 2021

Notes:
  Must either be run from anaconda command prompt or spyder, in a dedicated cosole.
  Required packages and commands to install:
    python -m pip install opencv-python
    python -m pip install pysimplegui
